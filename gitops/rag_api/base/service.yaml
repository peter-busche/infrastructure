apiVersion: v1
kind: Service
metadata:
  name: rag-api
  namespace: apps
spec:
  type: ClusterIP
  selector:
    app: rag-api
  ports:
    - protocol: TCP
      name: http
      port: 8000
      targetPort: 8000
